# PM Assistant 密钥配置指南

> 本指南涵盖代码同步到 GitHub 及 Streamlit Cloud 部署所需的全部密钥配置。
> 所有密钥均用占位符表示，**切勿将真实密钥粘贴到聊天或文档中**。

---

## 一、GitHub Personal Access Token（用于代码同步）

### 1. 生成步骤

1. 登录 https://github.com/settings/tokens
2. 点击右上角 **"Generate new token" → "Generate new token (classic)"**
3. 填写：
   - **Note（备注）**：`PM_Assistant_Sync`（随便写，用于自己识别）
   - **Expiration（过期时间）**：选 `30 days` 或 `No expiration`
   - **勾选权限**：只勾 `repo`（完整的仓库访问权限）
4. 点击 **"Generate token"**
5. **立即复制生成的 token**（形如 `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`），关闭页面后无法再查看

### 2. 使用方式（二选一）

**方式 A：本机安装 git 后推送**
```powershell
# 安装 git（仅需一次）
winget install Git.Git

# 初始化仓库并关联远程（在 PM_assident 目录下执行）
git init
git add .
git commit -m "feat: 优化招呼语/智能切块/PPT生成/一键导出"
git branch -M main
git remote add origin https://github.com/kjlsastop-cyber/PM_Assistant.git
git push -u origin main
# 推送时 GitHub 会弹出浏览器授权，用刚才生成的 token 登录即可
```

**方式 B：直接在浏览器上传（最简单，无需 git）**
1. 打开 https://github.com/kjlsastop-cyber/PM_Assistant/upload/main
2. 把要更新的文件（app.py、kb.py、styles/app.css、prompts/ 下两个 md、.env.example）拖进去
3. 底部点 **"Commit changes"** 即可

---

## 二、.env 配置（本地运行 / Streamlit Cloud 部署）

### 本地开发

把 `.env.example` 复制为 `.env`，然后按需填写：

```bash
copy .env.example .env
```

### Streamlit Cloud 部署

1. 打开 https://streamlit.io/cloud
2. 连接 GitHub 仓库 `kjlsastop-cyber/PM_Assistant`
3. 进入项目 → **Settings → Secrets**
4. 把下方 TOML 模板**整段粘贴**进去，将引号内内容替换为你的真实密钥
5. 点 **Save**，然后在应用菜单（⋮）里 **Reboot app**

> 注意：Secrets 是 **TOML 格式**，不是 .env 格式！
> - 值必须用**英文双引号**包起来：`KEY = "value"`（等号两边有空格）
> - 不能写成 `KEY=value`（这是 .env 格式，粘贴会报解析错误）
> - 所有键都写在**最外层**（不要加 `[分组]` 表头），根级键才会注入为环境变量

```toml
# ===== 对话模型（DeepSeek）=====
OPENAI_API_KEY = "sk-你的DeepSeek密钥"
OPENAI_BASE_URL = "https://api.deepseek.com/v1"
MODEL_NAME = "deepseek-chat"

# ===== 向量化服务（通义 DashScope）=====
EMBEDDING_API_KEY = "sk-你的DashScope密钥"
EMBEDDING_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"
EMBEDDING_MODEL = "text-embedding-v3"

# ===== 通义千问聊天模型 =====
QWEN_MODEL = "qwen3.8-max"

# ===== Rerank 重排（使用智谱，不需要可删掉这几行）=====
RERANK_API_KEY = "你的智谱密钥"
RERANK_MODEL = "rerank"
RERANK_CANDIDATES = 20
```

### 完整密钥清单

#### 必配（3 项）

| 密钥名 | 说明 | 示例 |
|---|---|---|
| `OPENAI_API_KEY` | 对话模型密钥 | `sk-xxxxxxxxxx` |
| `OPENAI_BASE_URL` | 对话模型 API 地址 | `https://api.deepseek.com/v1` |
| `MODEL_NAME` | 对话模型名 | `deepseek-chat` |

#### 必配（向量化，3 项）

| 密钥名 | 说明 | 示例 |
|---|---|---|
| `EMBEDDING_API_KEY` | Embedding 服务密钥 | `sk-xxxxxxxxxx` |
| `EMBEDDING_BASE_URL` | Embedding 服务地址 | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| `EMBEDDING_MODEL` | Embedding 模型名 | `text-embedding-v3` |

#### 可选

| 密钥名 | 说明 | 默认值 |
|---|---|---|
| `QWEN_MODEL` | 侧边栏可切换的通义模型 | `qwen3.8-max` |
| `RERANK_API_KEY` | Rerank 重排密钥（智谱，留空关闭） | （空） |
| `RERANK_MODEL` | Rerank 重排模型 | `rerank` |
| `RERANK_BASE_URL` | Rerank 服务地址 | `https://open.bigmodel.cn/api/paas/v4/rerank` |
| `RERANK_CANDIDATES` | 送入 Rerank 的候选数 | `20` |
| `KB_CHUNK_SIZE` | 知识库分块大小 | `700` |
| `KB_CHUNK_OVERLAP` | 分块重叠字符数 | `100` |
| `KB_TOP_K` | 检索返回条数 | `4` |
| `KB_MIN_SCORE` | 最低相似度阈值 | `0.25` |
| `REVIEW_ENABLED` | 是否启用自我审查 | `true` |
| `REVIEWER_MODEL` | 审查模型（留空复用对话模型） | （空） |
| `REVIEW_TEMPERATURE` | 审查温度 | `0.3` |
| `MAX_HISTORY_ROUNDS` | 历史对话保留轮数 | `10` |
| `MAX_HISTORY_CHARS` | 历史对话保留字符数 | `8000` |

### 推荐配置示例

```
# 对话模型（DeepSeek）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_BASE_URL=https://api.deepseek.com/v1
MODEL_NAME=deepseek-chat

# 向量化服务（通义 DashScope，与下方共用密钥）
EMBEDDING_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_MODEL=text-embedding-v3

# 通义千问
QWEN_MODEL=qwen3.8-max

# Rerank
RERANK_API_KEY=你的智谱密钥
RERANK_MODEL=rerank
RERANK_CANDIDATES=20
```

---

## 三、安全注意事项

1. **GitHub Token** 仅用于本次代码同步，用完可到 https://github.com/settings/tokens 吊销
2. **不要把密钥提交到 Git**：`.env` 已在 `.gitignore` 中排除
3. **Streamlit Cloud 的 Secrets** 仅在服务端可见，不会暴露
4. 轮换密钥时，只需在对应平台重新生成并更新配置即可
